<Window x:Class="圆角透明Window.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525"
        WindowStyle="None" AllowsTransparency="True" Background="{x:Null}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border CornerRadius="25,25,0,0" Background="Blue" Opacity="0.6" Grid.Row="0">
            <StackPanel Height="100"></StackPanel>
        </Border>
        <Border CornerRadius="0,0,25,25" Background="Orange" Opacity="0.6" Grid.Row="1">
            <StackPanel></StackPanel>
        </Border>
    </Grid>
</Window>
